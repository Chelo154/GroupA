@model IEnumerable<GroupA.Dominio.Sector>

@{
    ViewBag.Title = "Menu Sectores";
}

<h2 class="letraFormal text-center">Sectores</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr class="letraFormal">
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>Opciones</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            <button class="btn btn-default">
                @Html.ActionLink("Edit", "Edit", new { id = item.Id })
            </button>
            <button class="btn btn-default">
                @Html.ActionLink("Details", "Details", new { id = item.Id })
            </button>
            <button class=" btn btn-default">
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </button>
            <button class="btn btn-default">
                @Html.ActionLink("Agrear Empleados", "AgregarEmpleado", new { id = item.Id})
            </button>
           
        </td>
    </tr>
}

</table>
<link href="~/Content/FondosParaLetras.css" rel="stylesheet" />
